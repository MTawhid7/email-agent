# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
*.egg-info/

# Secrets — NEVER commit these
.env
# user's personal Gmail access token — never shared
credentials/token.json
data/

# credentials.json IS committed — it's the shared OAuth app identity, not a personal token

# Build output
build/
dist/
*.spec.bak

# OS
.DS_Store
Thumbs.db

# Logs
*.log
email_agent.log
